import tkinter as tk

window = tk.Tk()
window.title("AI Public Health Chatbot")
window.geometry("400x200")

label = tk.Label(window, text="Welcome to AI Public Health Chatbot")
label.pack(pady=20)

window.mainloop()